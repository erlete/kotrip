// ---------------------------------------------------------------------------
// Viaje (Trip)
// ---------------------------------------------------------------------------

/** Longitud mínima del nombre de un viaje. */
export const MIN_TRIP_NAME_LENGTH = 3;

/** Longitud máxima del nombre de un viaje. */
export const MAX_TRIP_NAME_LENGTH = 100;

/** Longitud máxima de la descripción de un viaje. */
export const MAX_TRIP_DESCRIPTION_LENGTH = 2000;

/** Puntuación mínima de un viaje (escala 1-10). */
export const MIN_RATING = 1;

/** Puntuación máxima de un viaje (escala 1-10). */
export const MAX_RATING = 10;

// ---------------------------------------------------------------------------
// Miembro (TripMember)
// ---------------------------------------------------------------------------

/** Longitud máxima del rol decorativo de un miembro del viaje. */
export const MAX_DECORATIVE_ROLE_LENGTH = 50;

// ---------------------------------------------------------------------------
// Itinerario (TripItinerary)
// ---------------------------------------------------------------------------

/** Longitud máxima del nombre de una parada del itinerario. */
export const MAX_ITINERARY_NAME_LENGTH = 200;

// ---------------------------------------------------------------------------
// Ticket (TripItineraryTicket)
// ---------------------------------------------------------------------------

/** Longitud máxima del nombre de un ticket. */
export const MAX_TICKET_NAME_LENGTH = 200;

/** Longitud máxima de la descripción de un ticket. */
export const MAX_TICKET_DESCRIPTION_LENGTH = 1000;

/** Longitud máxima de la URL del objeto asociado a un ticket. */
export const MAX_TICKET_URL_LENGTH = 2048;
